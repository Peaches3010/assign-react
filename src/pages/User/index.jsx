import React from "react";

const User = (props) => {
  return (
    <div className="container">
      <button type="submit" className="btn btn-primary flex-center mb-2">
        <ion-icon name="person-add" /> ADD
      </button>
    </div>
  );
};

export default User;
